version: "3"
services:
    postgres:
        container_name: laravelapi
        image: postgres:latest
        ports:
            - "5432:5432"
        volumes:
            - /data/laravelapi:/data/postgres
        env_file:
            - docker.env
        networks:
            - postgres


networks:
    postgres:
        driver: bridge
